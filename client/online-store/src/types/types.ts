export type brandsType = {
    id: number,
    name: string
}

export type userType = {
    id: number,
    email: string,
    password: string,
    role: string
}

export type typeOfDevice = {
    id: number,
    name: string
}